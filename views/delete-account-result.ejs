<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Account Deletion Result - SmartTailor NG</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        
        .container {
            background: white;
            border-radius: 20px;
            padding: 40px;
            max-width: 500px;
            width: 100%;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            text-align: center;
        }
        
        .icon {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            margin: 0 auto 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 40px;
        }
        
        .success .icon {
            background: #10B981;
            color: white;
        }
        
        .error .icon {
            background: #EF4444;
            color: white;
        }
        
        h1 {
            font-size: 28px;
            margin-bottom: 10px;
            color: #111827;
        }
        
        .success h1 {
            color: #10B981;
        }
        
        .error h1 {
            color: #EF4444;
        }
        
        .message {
            font-size: 16px;
            color: #6B7280;
            margin-bottom: 30px;
            line-height: 1.6;
        }
        
        .info-box {
            background: #F9FAFB;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 30px;
            text-align: left;
        }
        
        .info-box p {
            color: #6B7280;
            font-size: 14px;
            line-height: 1.6;
            margin: 0;
        }
        
        .button {
            background: #111827;
            color: white;
            border: none;
            padding: 14px 28px;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            width: 100%;
            transition: background 0.2s;
            margin-top: 10px;
        }
        
        .button:hover {
            background: #374151;
        }
        
        .button:active {
            transform: scale(0.98);
        }
        
        .button-secondary {
            background: #E5E7EB;
            color: #374151;
        }
        
        .button-secondary:hover {
            background: #D1D5DB;
        }
        
        @media (max-width: 600px) {
            .container {
                padding: 30px 20px;
            }
            
            h1 {
                font-size: 24px;
            }
        }
    </style>
</head>
<body>
    <div class="container <%= success ? 'success' : 'error' %>">
        <div class="icon">
            <% if (success) { %>
                ✓
            <% } else { %>
                ✕
            <% } %>
        </div>
        <h1>
            <% if (success) { %>
                Account Deleted Successfully
            <% } else { %>
                Account Deletion Failed
            <% } %>
        </h1>
        <p class="message"><%= message %></p>
        
        <% if (success) { %>
        <div class="info-box">
            <p>
                <strong>Your account and all associated data have been permanently deleted.</strong>
                <br><br>
                We're sorry to see you go. If you change your mind, you can always create a new account.
            </p>
        </div>
        <button class="button" onclick="window.close()">Close</button>
        <% } else { %>
        <div class="info-box">
            <p>
                <strong>Your account could not be deleted.</strong>
                <br><br>
                Please check the error message above and try again. If the problem persists, please contact support.
            </p>
        </div>
        <button class="button" onclick="window.history.back()">Go Back</button>
        <button class="button button-secondary" onclick="window.close()">Close</button>
        <% } %>
    </div>
    
    <script>
        <% if (success) { %>
        // Auto-close after 10 seconds if successful
        setTimeout(() => {
            try {
                window.close();
            } catch (e) {
                console.log('Cannot auto-close window. Please close manually.');
            }
        }, 10000);
        <% } %>
    </script>
</body>
</html>

