<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Result - SmartTailor NG</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        
        .container {
            background: white;
            border-radius: 20px;
            padding: 40px;
            max-width: 500px;
            width: 100%;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            text-align: center;
        }
        
        .icon {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            margin: 0 auto 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 40px;
        }
        
        .success .icon {
            background: #10B981;
            color: white;
        }
        
        .error .icon,
        .failed .icon {
            background: #EF4444;
            color: white;
        }
        
        .cancelled .icon {
            background: #F59E0B;
            color: white;
        }
        
        .pending .icon {
            background: #3B82F6;
            color: white;
        }
        
        h1 {
            font-size: 28px;
            margin-bottom: 10px;
            color: #111827;
        }
        
        .success h1 {
            color: #10B981;
        }
        
        .error h1,
        .failed h1 {
            color: #EF4444;
        }
        
        .cancelled h1 {
            color: #F59E0B;
        }
        
        .pending h1 {
            color: #3B82F6;
        }
        
        .message {
            font-size: 16px;
            color: #6B7280;
            margin-bottom: 20px;
            line-height: 1.6;
        }
        
        .return-instruction {
            background: #EFF6FF;
            border-left: 4px solid #3B82F6;
            padding: 16px;
            margin: 20px 0;
            border-radius: 8px;
            text-align: left;
        }
        
        .return-instruction strong {
            color: #1E40AF;
            display: block;
            margin-bottom: 8px;
            font-size: 14px;
        }
        
        .return-instruction p {
            color: #1E40AF;
            font-size: 14px;
            line-height: 1.5;
            margin: 0;
        }
        
        .details {
            background: #F9FAFB;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 30px;
            text-align: left;
        }
        
        .detail-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
        }
        
        .detail-row:last-child {
            margin-bottom: 0;
        }
        
        .detail-label {
            color: #6B7280;
            font-size: 14px;
        }
        
        .detail-value {
            color: #111827;
            font-weight: 600;
            font-size: 14px;
        }
        
        .tx-ref {
            background: #F3F4F6;
            padding: 12px;
            border-radius: 8px;
            margin: 20px 0;
            font-size: 12px;
            color: #6B7280;
            word-break: break-all;
        }
        
        .tx-ref strong {
            color: #374151;
            display: block;
            margin-bottom: 4px;
        }
        
        .button {
            background: #111827;
            color: white;
            border: none;
            padding: 14px 28px;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            width: 100%;
            transition: background 0.2s;
            margin-top: 10px;
        }
        
        .button:hover {
            background: #374151;
        }
        
        .button:active {
            transform: scale(0.98);
        }
        
        .button-secondary {
            background: #E5E7EB;
            color: #374151;
        }
        
        .button-secondary:hover {
            background: #D1D5DB;
        }
        
        @media (max-width: 600px) {
            .container {
                padding: 30px 20px;
            }
            
            h1 {
                font-size: 24px;
            }
        }
    </style>
</head>
<body>
    <div class="container <%= success ? 'success' : (paymentStatus || 'error') %>">
        <div class="icon">
            <% if (success) { %>
                ✓
            <% } else if (paymentStatus === 'cancelled') { %>
                ⚠
            <% } else if (paymentStatus === 'failed') { %>
                ✕
            <% } else if (paymentStatus === 'pending') { %>
                ⏳
            <% } else { %>
                ✕
            <% } %>
        </div>
        <h1>
            <% if (success) { %>
                Payment Successful!
            <% } else if (paymentStatus === 'cancelled') { %>
                Payment Cancelled
            <% } else if (paymentStatus === 'failed') { %>
                Payment Failed
            <% } else if (paymentStatus === 'pending') { %>
                Payment Pending
            <% } else { %>
                Payment Error
            <% } %>
        </h1>
        <p class="message"><%= message %></p>
        
        <% if (success && subscriptionType && amount) { %>
        <div class="details">
            <div class="detail-row">
                <span class="detail-label">Subscription Type:</span>
                <span class="detail-value"><%= subscriptionType.charAt(0).toUpperCase() + subscriptionType.slice(1) %></span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Amount Paid:</span>
                <span class="detail-value">₦<%= typeof amount === 'number' ? amount.toLocaleString() : amount %></span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Status:</span>
                <span class="detail-value" style="color: #10B981;">Active</span>
            </div>
        </div>
        
        <div class="return-instruction">
            <strong>📱 Return to App</strong>
            <p>Your subscription has been activated successfully! Please return to the SmartTailor NG app to continue using all features.</p>
        </div>
        <% } %>
        
        <% if (txRef) { %>
        <div class="tx-ref">
            <strong>Transaction Reference:</strong>
            <%= txRef %>
        </div>
        <% } %>
        
        <% if (success) { %>
        <button class="button" onclick="window.close()">Close & Return to App</button>
        <% } else if (paymentStatus === 'cancelled' || paymentStatus === 'failed') { %>
        <button class="button" onclick="window.close()">Close</button>
        <button class="button button-secondary" onclick="window.history.back()" style="margin-top: 10px;">Try Again</button>
        <% } else { %>
        <button class="button" onclick="window.close()">Close</button>
        <% } %>
    </div>
    
    <script>
        <% if (success) { %>
        // Show success message and auto-close after 10 seconds
        console.log('Payment successful! Subscription activated.');
        
        // Try to close the window after 10 seconds
        setTimeout(() => {
            try {
                window.close();
            } catch (e) {
                console.log('Cannot auto-close window. Please close manually.');
            }
        }, 10000);
        <% } %>
    </script>
</body>
</html>
